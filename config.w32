// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("google_authenticator", "for google_authenticator support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("google_authenticator", "enable google_authenticator support", "no");

if (PHP_GOOGLE_AUTHENTICATOR != "no") {
	EXTENSION("google_authenticator", "google_authenticator.c");
}

